const logSchema = require("./log");
const userSchema = require("./user");
const requestSchema = require("./request");
const widgetParametersSchema = require("./widgetParameters");

module.exports = {
  logSchema,
  userSchema,
  requestSchema,
  widgetParametersSchema,
};
